name: Windows 10 tiny noVNC Tailscale (8-Core 16GB 356GB)

on:
  workflow_dispatch:

jobs:
  secure-windows:
    runs-on: ubuntu-22.04
    timeout-minutes: 350

    steps:
      - name: Enable KVM Hardware Acceleration
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-kvm net-tools
          sudo chmod 666 /dev/kvm

      - name: Install Tailscale
        run: |
          curl -fsSL https://tailscale.com/install.sh | sh

      - name: Establish Tailscale Connection
        run: |
          sudo tailscale up --authkey=${{ secrets.TAILSCALE_AUTHKEY }} --hostname=gh-runner-$GITHUB_RUN_ID
          
          TS_IP=""
          RETRIES=0
          while [ -z "$TS_IP" ] && [ $RETRIES -lt 10 ]; do
              TS_IP=$(tailscale ip -4)
              sleep 5
              RETRIES=$((RETRIES+1))
          done
          
          if [ -z "$TS_IP" ]; then
              echo "Tailscale IP not assigned. Exiting."
              exit 1
          fi
          
          echo "TAILSCALE_IP=$TS_IP" >> $GITHUB_ENV

      - name: Run Windows 10 Pro Container
        run: |
          USERNAME="MXDEV"
          PASSWORD="MXDEV123"
          
          echo "RDP_USER=$USERNAME" >> $GITHUB_ENV
          echo "RDP_PASS=$PASSWORD" >> $GITHUB_ENV

          docker run -d --name windows \
            -e VERSION="tiny10" \
            -e USERNAME="$USERNAME" \
            -e PASSWORD="$PASSWORD" \
            -e AUTOLOGIN="N" \
            -e CPU_CORES="12" \
            -e RAM_SIZE="32G" \
            -e DISK_SIZE="512G" \
            -p 8006:8006 \
            -p 3389:3389 \
            --device=/dev/kvm \
            --device=/dev/net/tun \
            --cap-add NET_ADMIN \
            --stop-timeout 120 \
            docker.io/dockurr/windows

      - name: Maintain Connection
        run: |
          echo -e "\n======================================================="
          echo " This session is brought to you by MX DEV!"
          echo " Subscribe to our Telegram channel for more methods:"
          echo " https://t.me/mxdevrdp"
          echo -e "=======================================================\n"
          
          echo -e "\n=== WINDOWS 10 TINY CONNECT INFO ==="
          echo "Direct Web noVNC: http://$TAILSCALE_IP:8006"
          echo "RDP Address:      $TAILSCALE_IP:3389"
          echo "-------------------------------------------------------"
          echo "Username:         $RDP_USER"
          echo "Password:         $RDP_PASS"
          echo "Autologin:        DISABLED (Password Required)"
          echo "Resources:        12 CPU Cores | 32 GB RAM"
          echo "System Disk:      512GB (Single Drive)"
          echo "=======================================================\n"
          
          while true; do
              echo "[$(date)] Windows 10 tiny Active - MX DEV (https://t.me/mxdevrdp)"
              sleep 300
          done

